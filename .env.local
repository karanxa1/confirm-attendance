# Using production database exclusively
REACT_APP_FIREBASE_ENV=production

# Firebase Admin Environment Variables
# Used as a fallback when the service account file isn't available

# Firebase project ID
FIREBASE_PROJECT_ID=sample-firbase-ai-app-c1fc3

# Do NOT put the actual Firebase Admin service account JSON here for production
# This is just a reference that the value should be set in Vercel dashboard
FIREBASE_SERVICE_ACCOUNT={"type":"service_account",...}
